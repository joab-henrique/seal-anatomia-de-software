{
  "name": "seal-anatomia-de-software",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Experiência interativa de engenharia de software para o público da SEAL / CIn–UFPE",
  "scripts": {
    "start": "node server.cjs",
    "dev": "node server.cjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "lucide": "^0.468.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
